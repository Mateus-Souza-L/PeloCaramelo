# Porta que o servidor Express vai usar
PORT=4000

# URL de conexão do PostgreSQL
# Exemplo local:
#   postgresql://USUARIO:SENHA@HOST:PORTA/NOME_BANCO
# Exemplo em cloud (Render / Supabase / Railway):
#   postgres://USUARIO:SENHA@HOST:PORTA/NOME_BANCO
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD_HERE@localhost:5432/pelocaramelo

# Se estiver usando banco local, deixe "false"
# Em cloud (Render / Railway / Supabase), normalmente é "true"
DB_SSL=false

# Segredo usado para assinar tokens JWT
# Em produção, use algo bem forte e gerado aleatoriamente
JWT_SECRET=change-this-secret-in-production
